"use client";

import React from 'react';

export function AiToolTabs({
  tools,
  defaultTool,
  section,
  style,
}: {
  tools: string[];
  defaultTool: string;
  section: string;
  style?: React.CSSProperties;
}) {
  return null;
}
